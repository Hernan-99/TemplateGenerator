[build]
  base = "."
  command = "cd Frontend && npm install && npm run build -- --configuration=production"
  publish = "Frontend/dist/frontend"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
